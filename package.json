{
  "name": "travel-guide",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Travel Guide application",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd client && npm install --legacy-peer-deps && npm install vite@latest @vitejs/plugin-react --save-dev && npx vite build",
    "vercel-build": "cd client && npm install --legacy-peer-deps && npm install vite@latest @vitejs/plugin-react --save-dev && npx vite build"
  }
} 